spring:
  application:
    name: user
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
    stream:
      kafka:
        binder:
          brokers: http://127.0.0.1:9092
#服务注册配置
eureka:
  client:
    service-url:
      defaultZone: http://localhost:20200/eureka/



